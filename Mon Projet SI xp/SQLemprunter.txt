
CREATE table "NOTIFICATION" (
    "ID_STOCK_DE"    NUMBER(10) NOT NULL,
    "ID_STOCK_VERS"  NUMBER(10) NOT NULL,
    "ID_DEMANDE" NUMBER(10,0) NOT NULL,
    "MESSAGE"    VARCHAR2(4000) NULL,
    "AREPONDRE"  NUMBER(10,0) NOT NULL,
    CONSTRAINT "NOTIFICATION_PK" PRIMARY KEY ("ID_STOCK_DE" , "ID_STOCK_VERS", "ID_DEMANDE") ENABLE
)
/

ALTER TABLE "NOTIFICATION" ADD CONSTRAINT "NOTIFICATION_Demande" 
FOREIGN KEY ("ID_DEMANDE")
REFERENCES "DEMANDE" ("ID_DEMANDE")

/
ALTER TABLE "NOTIFICATION" ADD CONSTRAINT "id_stock_DE_cs" 
FOREIGN KEY ("ID_STOCK_DE")
REFERENCES "STOCK" ("ID_STOCK")

/
ALTER TABLE "NOTIFICATION" ADD CONSTRAINT "id_stock_VERS_cs" 
FOREIGN KEY ("ID_STOCK_VERS")
REFERENCES "STOCK" ("ID_STOCK")

/




